<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodTiger"
        tools:targetApi="31">

        <activity
            android:name=".DapitanDetailed"
            android:exported="false" />

        <activity
            android:name=".DapitanMainList"
            android:exported="true">
        </activity>

        <activity
            android:name=".EspanaDetailed"
            android:exported="false" />

        <activity
            android:name=".EspanaMainList"
            android:exported="true">
        </activity>

    <activity
        android:name=".LacsonDetailed"
        android:exported="false" />

    <activity
        android:name=".LacsonMainList"
        android:exported="true">
    </activity>

    <activity
        android:name=".PnovalDetailed"
        android:exported="false" />

    <activity
        android:name=".PnovalMainList"
        android:exported="true">
    </activity>

        <activity
            android:name=".Categories"
            android:exported="true">s

    <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>

        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">


        </activity>
    </application>

</manifest>